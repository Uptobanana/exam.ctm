# 跨科联动补齐工程 · 施工设计（更新版）

**日期**：2026-08-06 初版 / 2026-08-07 更新
**目标**：补齐s1中基ID格式统一 + s2中诊/s5内科/s9针灸跨科标记，为A/B/C跨科联动检索奠定基础

---

## 〇、更新记录（批次1执行后并入）

| 更新项 | 原设计 | 修正后 | 依据 |
|--------|--------|--------|------|
| s1统一格式 | `1-1-1`->`zji-1-1`（两段） | `1-1-1`->`zji-1-1-1`（**三段，加前缀**） | s1实际是三段`单元-子单元-序号`（13单元/142点），字面两段化会ID碰撞 |
| 中基标记规范 | `[zji-单元-序号]`（两段） | `[zji-单元-子单元-序号]`（**三段**，如`[zji-1-2-3]`） | 与s1统一后ID三段式对应 |
| s2 derive-box数 | 97个 | **97个（原设计正确）** | Python`str.count`核实；`grep -c`按行计数在压缩JS上失真误读96 |
| s5 derive-box数 | 62个 | **62个（原设计正确）** | Python`str.count`核实；`grep -c`误读52 |
| s2方剂实际数 | 42种/677处 | **97种/1027处** | 从s4解析128方剂,97种在s2出现,共1027处提及(原设计42/677为不完整估计) |
| s9引号风格 | "用单引号content格式" | id用**双引号**`{id:"zj-1-1"}`，content字段单引号 | `grep`核实 |
| 批次1状态 | 待执行 | ✅ **已完成**（205处，2026-08-07） | node --check通过+六重验证 |
| 批次2状态 | 待执行 | ✅ **已完成**（1027处标记，2026-08-07） | node --check通过+八重验证 |
| s5方剂实际数 | 35种/541处 | **82种/1142处(content内835)** | 从s4解析128方剂,82种在s5出现;1142处中835在content、307在cardQuiz/练习题(按约定不标记) |
| 批次3状态 | 待执行 | ✅ **已完成**（835处标记，2026-08-07） | node --check通过+八重验证;content-only |

**新增技术风险**：见第二节"限制7/8"（ID碰撞风险、文档过期风险）。

**重要提醒**：项目交接手册等记忆已严重过期（24-52天前），记录的文件大小与现状出入很大。s5/s6/s7/s8均已填充完成（非"待填充"骨架），s9已121KB（非55.7KB）。一切以实际文件核对为准。

---

## 一、补齐任务清单与影响范围

### 1.1 s1中基ID格式统一（`1-1-1` -> `zji-1-1-1`，三段加前缀）✅已完成

| 影响项 | 数量 | 说明 |
|--------|------|------|
| 知识点`id`字段 | 142处 | `{id:'1-1-1'` -> `{id:'zji-1-1-1'` |
| 练习题`pointId`引用 | 63处 | `pointId:'1-1-1'` -> `pointId:'zji-1-1-1'` |
| **总改动** | **205处** | 全部在s1-zhongji.js文件内 |
| app.js影响 | **0** | `findPoint(id)`/`getPointPath(id)`用`===`全量遍历匹配，不解析ID前缀（已核实源码） |
| index.html影响 | **0** | 只引用文件名`s1-zhongji.js`，不引用知识点ID |

**风险**：低。s1的ID是三段式`单元-子单元-序号`（13个单元、142个知识点，如`1-1-1`/`5-2-10`/`13-2-6`），与`findPoint`遍历的`units.subunits.points`三级结构对应。统一方式为**加`zji-`前缀保留三段**，142个原ID本就唯一，加前缀后仍唯一，零碰撞。

> ⚠️ **为何不能用两段**：原设计示例`1-1-1`->`zji-1-1`（去掉首位数字）会使`1-1-1`与`2-1-1`都变成`zji-1-1`而碰撞，导致`findPoint`返回错误知识点、知识树/练习题/收藏全错乱。且"正则替换"方法只能做加前缀或丢首位，无法做"按单元内顺序重编号"。故采用加前缀保三段（用户已确认）。

### 1.2 s2中诊补跨科标记

| 影响项 | 数量 | 说明 |
|--------|------|------|
| derive-box数 | 97个 | 已有derive-box，需在content中加标记 |
| 方剂文字提及 | 1027处(97种方剂) | 如"肾气丸"->"肾气丸[fj-8-3]"；原设计估677处/42种为不完整统计 |
| 中药文字提及 | ~60处 | 可选择性加`[zy-X-Y]`标记 |

**风险**：低。在现有content字符串内加标记文本，不改变JS结构。标记格式`[fj-X-Y]`本身无单引号，不与`content:'...'`的单引号冲突。注意避免重复标记（同一方剂名后不要重复追加）。

### 1.3 s5内科补跨科标记

| 影响项 | 数量 | 说明 |
|--------|------|------|
| derive-box数 | 62个 | 已有derive-box，需在content中加标记 |
| 方剂文字提及 | 835处content(82种方剂) | 如"归脾汤"->"归脾汤[fj-8-3]"；另有307处在cardQuiz/练习题中按约定不标记；原设计估541处/35种为不完整统计 |
| 中药文字提及 | ~735处 | 可选择性加`[zy-X-Y]`标记 |

**风险**：低。同s2中诊。

### 1.4 s9针灸derive-box+跨科标记

| 影响项 | 数量 | 说明 |
|--------|------|------|
| 知识点数 | 41个 | 0个derive-box，需从0做 |
| 单元数 | 28个 | 经络总论+十四经穴+刺灸法+治疗各论 |
| 现有元件 | classic-quote(41)/compare-table(29)/trap-box(40)/plain(57)/mnem(40)/cardQuiz(35) | 基础元件齐全，仅缺derive-box（均已核对一致） |

**风险**：中。工作量大（41个derive-box从0做），但结构复用已验证模式（总论概念型+各论推理链）。
> ⚠️ **引号风格**：s9的`id`字段用**双引号**`{id:"zj-1-1"}`，`content`字段用单引号`content:'...'`。做derive-box时content仍用单引号包裹，内部避免单引号。

---

## 二、技术限制

### ⚠️ 限制1：文件截断
所有JS文件用Python脚本+bash写入，不用Write工具写大文件。

### ⚠️ 限制2：单引号冲突
- s1/s2/s5的`content`用单引号包裹；s9的`content`单引号、`id`双引号
- derive-box/标记内容中避免单引号，用双引号或省略
- 标记格式`[fj-X-Y]`/`[zy-X-Y]`/`[zji-X-Y-Z]`本身无单引号，安全

### ⚠️ 限制3：跨行content字符串
JS字符串不能物理跨行。Python构建时压缩为一行。

### ⚠️ 限制4：语法验证
每批次后`node --check`验证，通过才继续。

### ⚠️ 限制5：内容只做加法
s1中基改ID是"加前缀"（ID格式统一），现有内容一字不减。s2/s5加标记是"在现有文字后追加标记"，不改原有文字。s9新增derive-box。

### ⚠️ 限制6：标记格式规范
- 方剂标记：`[fj-单元-序号]`（如`[fj-8-3]`），与s4方剂学知识点ID对应
- 中药标记：`[zy-单元-序号]`（如`[zy-5-1]`），与s3中药学知识点ID对应
- **中基标记：`[zji-单元-子单元-序号]`（三段，如`[zji-1-2-3]`）**，与s1中基（统一后）知识点ID对应 ⚠️三段非两段
- 中诊标记：`[dx-单元-序号]`（如`[dx-2-1]`），与s2中诊知识点ID对应
- 内科标记：`[nk-单元-序号]`（如`[nk-1-1]`），与s5内科知识点ID对应
- 针灸标记：`[zj-单元-序号]`（如`[zj-1-1]`），与s9针灸知识点ID对应

### ⚠️ 限制7：ID碰撞风险（批次1发现）
替换ID时必须保证唯一性。加前缀类操作安全；任何"丢数字段"或"重编号"操作必须先验证无碰撞（去重计数==原计数）。`findPoint`用`===`全量匹配，碰撞会静默返回错误知识点。

### ⚠️ 限制8：文档/记忆过期风险（批次1发现）
交接手册等记忆已24-52天未更新，文件大小、科目状态多处过期（如s5记为12KB骨架，实际340KB已填充）。执行任何批次前，先`ls -la`+`grep`核对实际状态，不轻信记忆中的数字。

### ⚠️ 限制9：minified JS计数陷阱（批次2发现）
本项目的JS文件是压缩格式（每个content字符串是超长单行）。`grep -c`统计的是**匹配行数**而非**出现次数**，在压缩JS上会严重失真（如s2的derive-box实际97次，`grep -c`误读为96）。统计出现次数必须用 `grep -o ... | wc -l` 或 Python `str.count()`。批次1中我曾因此把s2/s5的derive-box数误"修正"为96/52，后用Python核实还原为97/62。

### ⚠️ 限制10：方剂映射需从s4解析（批次2发现）
s4方剂学的`name`字段是描述性文本（如"麻黄汤、桂枝汤、小青龙汤的组成、功用、主治及配伍意义"），**不是**单个方剂名。构建方剂->fj-ID映射需：跳过fj-1-*概念点，取name中"的"之前部分、按"、"分割得各方剂名。s4共128方剂（单元2-19）。s2中出现97种/1027处，s5待核。

### ⚠️ 限制11：标记已是项目既定约定（批次2确认）
`[fj-X-Y]`/`[zy-X-Y]`文本标记在s3/s4/s6/s7/s8已大量使用（s3有279个fj、s4有292个zy、s8有455个fj）。app.js暂未将其渲染为可点击链接（content直接innerHTML），标记目前以字面文本显示，属"奠基"阶段，后续统一实现链接渲染。s2/s5/s9是仅有的无标记缺口。

---

## 三、分批次执行计划

### 批次总览

| 批次 | 任务 | 文件 | 工作量 | 预计增量 | 状态 |
|------|------|------|--------|---------|------|
| **1** | s1中基ID格式统一 | s1-zhongji.js | 205处加前缀 | +820字节 | ✅已完成 |
| **2** | s2中诊补方剂标记 | s2-zhongzhen.js | 1027处加标记 | +8.6KB | ✅已完成 |
| **3** | s5内科补方剂标记 | s5-neike.js | 835处加标记(content) | +7KB | ✅已完成 |
| **4a** | s9针灸derive-box(总论) | s9-zhenjiu.js | 单元1-5(9点) | +15.4KB | ✅已完成 |
| **4b** | s9针灸derive-box(经穴) | s9-zhenjiu.js | 单元6-20(15点) | +26.6KB | ✅已完成 |
| **4c** | s9针灸derive-box(刺灸+治疗) | s9-zhenjiu.js | 单元21-28(14点) | +24.4KB | ✅已完成 |

### 批次1：s1中基ID格式统一 ✅已完成（2026-08-07）

**操作**：Python正则给s1-zhongji.js中所有三段数字ID加`zji-`前缀。
- `{id:'1-1-1'` -> `{id:'zji-1-1-1'`（142处知识点ID）
- `pointId:'1-1-1'` -> `pointId:'zji-1-1-1'`（63处练习题引用）

**执行记录**：
- 替换前：旧格式id 142、pointId 63、zji- 0（备份s1-zhongji.js.bak-20260807）
- Python正则替换：id 142处、pointId 63处，合计205处，文件增820字节=205×4（纯加前缀）
- 验证：`node --check`通过；新格式142+63=205；旧格式残留0；142个id去重后仍142（零碰撞）；s2/app.js未被波及

### 批次2：s2中诊补方剂标记 ✅已完成（2026-08-07）

**操作**：Python脚本在s2中诊的方剂名提及后追加`[fj-X-Y]`标记。

**方剂-标记映射表**（从s4-fangji.js解析构建，128方剂中97种在s2出现）：
> ⚠️ 原设计示例"麻黄汤->[fj-2-1] 桂枝汤->[fj-2-1]"不妥，映射应从s4源文件解析。s4的name字段是描述性文本（如"麻黄汤、桂枝汤、小青龙汤的组成..."），需跳过fj-1-*概念点、取"的"前按"、"分割得各方剂名（见限制10）。

**执行记录**：替换前[fj-标记0，备份s2-zhongzhen.js.bak-20260807；新增1027处标记，文件增8593字节（≈8.4字节/标记）。验证：`node --check`通过；标记数1027；零重复；嵌套正确（当归四逆汤[fj-7-2]×4，无错误[fj-7-1]）；抽查归脾汤/肾气丸/麻黄汤/银翘散/安宫牛黄丸映射全对；格式全规范；标记均在content内；s1/s3未波及。注意用`grep -o|wc -l`计数非`grep -c`（见限制9）。

**执行要点**：
- 先从s4-fangji.js提取全部方剂名及其`fj-N-N`ID，构建映射
- 按方剂名长度降序匹配（避免"桂枝汤"被"桂枝"类短名先匹配干扰——实际方剂名较少此问题，但长名优先更稳）
- 已有`[fj-`标记处不重复追加
- 标记插在方剂名之后、紧贴无空格

**验证**：
- `node --check s2-zhongzhen.js`
- `grep -c "\[fj-" s2-zhongzhen.js` 确认标记数与提及数相符
- 抽查若干处确认方剂名后紧跟正确fj-ID

### 批次3：s5内科补方剂标记 ✅已完成（2026-08-07）

**操作**：同批次2，从s4解析方剂映射，长名优先仅在`content:'...'`字面量内为方剂名追加`[fj-X-Y]`标记。

**执行记录**：s5引号风格同s9（id双引号`{id:"nk-1-1"}`、content单引号、无转义）。s5实际82种方剂/1142处提及，其中content内835处已标记、cardQuiz/练习题307处按s6/s7/s8既定约定(content-only)不标记（s6/s7/s8的fj标记100%在content内）。备份s5-neike.js.bak-20260807，新增835处标记，文件+7044字节。验证：`node --check`通过；标记数835；零重复；嵌套正确（枳实薤白桂枝汤[fj-12-2]×4，无错误内含桂枝汤标记）；抽查归脾汤/肾气丸/麻黄汤/银翘散/安宫牛黄丸/天麻钩藤饮/血府逐瘀汤/真武汤映射全对；835标记全在content内；s2/s4未波及。

**验证**：`node --check s5-neike.js` ✅；`grep -o "\[fj-" s5-neike.js | wc -l` ✅835；抽查映射 ✅

### 批次4a/4b/4c：s9针灸derive-box

**操作**：为s9针灸41个知识点新增derive-box，复用已验证模式：
- 总论(单元1-5)：概念阐述型4步（经络/腧穴总论）
- 经穴(单元6-20)：概念阐述型4步（十四经穴定位/主治）
- 刺灸法(单元21-23)：概念阐述型4步
- 治疗各论(单元24-28)：疾病推理链5步

**分3批**（4a/4b/4c），每批约10-16个知识点，控制脚本大小。
**验证**：每批`node --check`。

### 批次4a：s9总论derive-box ✅已完成（2026-08-07）

**操作**：为s9单元1-5的9个知识点（zj-1-1~zj-1-4、zj-2-1、zj-2-2、zj-5-1~zj-5-3；单元3/4无点）各撰写概念阐述型4步derive-box。

**derive-box结构**（从s2/s5既有derive-box提取的范式）：上部`derive-flow`四节点流程图（df-concept->df-logic->df-organ->df-symptom，用`<br>`分行），下部4个`derive-step`（ds-blue/ds-red/ds-purple/ds-orange），每步含ds-num/ds-title/ds-body。

**执行要点**：
- 双引号用`&#34;`实体（匹配s9既有约定），HTML属性用双引号，全文无单引号（不破坏s9单引号content包裹）
- s9 content含mini-quiz的`\'`转义，须用转义感知扫描定位content闭合`'`（不能用`content:'[^']*'`）
- 插入位置：各点content末尾、闭合`'`前
- 9个derive-box数据存outputs/s9_derive_data.py，插入脚本outputs/s9_derive_insert.py

**执行记录**：9个derive-box插入，s9 derive-box从0增至9，文件+15.4KB。验证：`node --check`通过；结构计数全对（9 box/9 flow/36 step=9×4，ds-blue/red/purple/orange各9）；9个目标点均有derive-box；derive-box无单引号；zj-5-3的mini-quiz转义保留完好；其他文件未波及，s9既有元件完好（classic-quote 41/trap-box 40）。备份outputs/s9-zhenjiu.js.bak-20260807。

**验证**：`node --check s9-zhenjiu.js` ✅；结构计数 ✅；抽查zj-1-1 derive-box 2088字、4步标题完整、16个`&#34;`实体 ✅

### 批次4b：s9经穴derive-box ✅已完成（2026-08-07）

**操作**：为s9单元6-20的15个知识点（zj-6-1~zj-20-1，覆盖手三阴三阳、足三阴三阳、督脉、任脉、经外奇穴）各撰写概念阐述型4步derive-box，内容侧重：经脉循行->主治规律（经脉所过主治所及）->常用穴定位与主治->易混点（五输穴特性、八会穴、下合穴/募穴、禁忌等）。

**执行要点**：15个derive-box约25KB超Write截断阈值，数据拆3文件（s9_derive_data_4b_1/2/3.py，每5点）合并插入。沿用4a标准：`&#34;`代双引号、无单引号、转义感知定位content闭合`'`、插入content末尾。

**执行记录**：15个derive-box插入，s9 derive-box 9->24，文件+26.6KB。验证：`node --check`通过；结构计数全对（24 box/24 flow/96 step=24×4，四色各24）；15个新点均有derive-box；derive-box无单引号（抽查zj-12-1 1822字、4步标题完整）；其他文件未波及，s9既有元件完好。备份outputs/s9-zhenjiu.js.bak-20260807-4b。

**验证**：`node --check s9-zhenjiu.js` ✅；结构计数 ✅；抽查zj-12-1 derive-box完整 ✅

### 批次4c：s9刺灸法+治疗各论derive-box ✅已完成（2026-08-07，工程竣工）

**操作**：为s9单元21-28的14个知识点撰写derive-box。
- **单元21-24（10点：刺灸法+治疗总论）用4步概念型**：体位/进针/角度/行针得气/补泻/晕针/灸法/电针三棱针/选穴原则/特定穴
- **单元25-28（4点：治疗各论）用5步推理链（病-因-机-证-法穴）**：痛证经络病(头痛落枕漏肩风腰痛痹证)/内科杂病(中风眩晕面瘫不寐感冒哮喘胃痛呕吐泄泻便秘)/妇科儿科(痛经崩漏缺乳遗尿)/皮外五官(瘾疹蛇串疮扭伤目赤耳鸣咽肿牙痛)
- 医学判断：单元24（选穴原则、特定穴应用）是治疗方法论非具体疾病，故用4步概念型（非设计的5步），更贴合内容。5步box颜色用蓝/红/紫/橙/蓝循环。

**执行记录**：14个derive-box插入（数据拆3文件s9_derive_data_4c_1/2/3.py合并），s9 derive-box 24->38（**全部38点覆盖**），文件+24.4KB。验证：`node --check`通过；结构计数全对（38 box/38 flow/156 step=34×4+4×5）；14新点均有derive-box；5步box抽查zj-25-1完整（1869字、5步标题：病-因-机-证-法穴、无单引号）；其他文件未波及；s9既有元件完好。备份outputs/s9-zhenjiu.js.bak-20260807-4c。

**验证**：`node --check s9-zhenjiu.js` ✅；结构计数 ✅；38点derive-box全覆盖 ✅；5步box抽查 ✅

---

## 🎉 工程竣工总结（2026-08-07）

跨科联动补齐工程全部6批次完成：
| 批次 | 成果 |
|------|------|
| 1 | s1中基ID统一205处（zji-三段式） |
| 2 | s2中诊补方剂标记1027处 |
| 3 | s5内科补方剂标记835处（content内） |
| 4a | s9总论derive-box 9个 |
| 4b | s9经穴derive-box 15个 |
| 4c | s9刺灸治疗derive-box 14个 |

s9 derive-box 38/38全覆盖。九科数据齐备，A/B/C跨科联动检索基础奠定。
**待办（设计6批次之外的可选项）**：验收标准5中s9"出站跨科标记"未做--s9是zj链接目标但无出站标记（其content以针灸穴位/手法为主，无自然方剂/中药提及可标）。若需在s9 content提及中基概念处加[zji-]等出站标记，属额外工作，可另议。

---

## 四、执行顺序与依赖

```
批次1(s1中基ID统一) ──✅已完成
批次2(s2中诊补标记) ──✅已完成
批次3(s5内科补标记) ──✅已完成
批次4a(s9总论derive-box) ──✅已完成
批次4b(s9经穴derive-box) ──✅已完成
批次4c(s9刺灸治疗derive-box) ──✅已完成
```

批次2/3/4a相互独立，可任意顺序执行。4b依赖4a（同一文件追加），4c依赖4b。

**建议执行顺序**：✅ 全部完成（批次1/2/3/4a/4b/4c均已竣工，2026-08-07）

---

## 五、验收标准

1. **s1中基**✅：142个知识点ID全部改为`zji-X-Y-Z`三段格式，63个练习题pointId同步更新，`node --check`通过，零碰撞
2. **s2中诊**✅：97种方剂提及处均有`[fj-X-Y]`标记（共1027处），`node --check`通过
3. **s5内科**✅：82种方剂content内提及处均有`[fj-X-Y]`标记（共835处，cardQuiz/练习题307处按s6/s7/s8约定不标记），`node --check`通过
4. **s9针灸**✅：38个知识点全部有derive-box（4a×9+4b×15+4c×14=38；刺灸法/治疗总论用4步概念型、治疗各论用5步推理链），`node --check`通过
5. **全科目跨科标记覆盖**：9科均有跨科标记（s1中基`zji`三段/s2中诊`dx`+`fj`/s3中药`zy`+`fj`/s4方剂`fj`+`zy`/s5内科`nk`+`fj`/s6外科`wk`+`fj`/s7妇科`fk`+`fj`/s8儿科`ek`+`fj`/s9针灸`zj`+跨科标记）
6. **现有功能不受影响**：知识树浏览/练习题/收藏/统计等功能正常（ID改动后findPoint仍可匹配）
