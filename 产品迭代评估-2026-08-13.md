# 传统医学出师考核 · 学习工具 — 项目掌握与迭代方向评估

> 评估日期：2026-08-13｜评估人：产品通（PM 视角）
> 结论先行：产品**功能骨架已非常完整且自研能力突出**（动态出题引擎是核心壁垒），但存在**三处"已设计/已写但未真正落地"的漂移**，以及**内容深度不均**问题。下一步优先级应放在"补完已承诺能力 + 内容均质化"，而非盲目加新功能。

---

## 一、项目是什么

一个**离线优先的纯前端 SPA**（原生 JS，无构建步骤），面向"传统医学出师考核"（师承/确有专长人员考试）备考者。入口 `index.html`，核心引擎 `app.js`（~1600 行 / 69KB）+ 9 个科目数据文件（`subjects/s1~s9.js`）。已部署于 CloudBase 静态托管，目标是在手机/通勤场景下离线刷题、看知识点。

**覆盖 9 科、664 个知识点、约 320 道静态题 + 动态生成题。**

## 二、完成度盘点（已核实）

| 模块 | 状态 | 说明 |
|------|------|------|
| 9 科知识点内容 | ✅ 完成 | 664 点；s1（中基）已深度改造（derive-box/probe-box 等） |
| 学习框架（树导航/卡片/收藏/统计/路线） | ✅ 完成 | app.js 承载 |
| 自适应练习 + 错题本 | ✅ 完成 | perf 表 + 三池加权 + localStorage 错题本 |
| **动态出题引擎（dynquiz.js）** | ✅ 批次1-5 | **核心壁垒**：9 科启用，6 类题型，去重降权、错题联动 |
| 跨科检索/链接（xlink.js A/B） | ✅ 批次0-4 | 侧栏搜索 + 卡片内可点跨科链接 + 导航面包屑 |
| 跨科知识图谱（xlink C 阶段） | ❌ 未做 | `xgraph.js` 缺失，但 `getNeighbors/getSubgraph` 接口已就绪 |
| PWA / 离线 / 可安装 | ⚠️ **失效** | manifest 在，但 `sw.js` 注册被移除 → 实际不能离线（见风险2） |
| 题库扩充（320→500+） | ❓ 未确认 | D 计划目标，是否在手头数据中完成未知 |
| **用户系统（云后端）** | ⚠️ **仅后端** | 云函数 login/register/initAdmin 真实可用，但前端 0% 集成（见风险1） |

## 三、核心优势（应保留并放大）

1. **动态出题引擎是真正的差异化能力**：无需为每个知识点手工命题，从内容自动生成 6 类题，并基于 `dynHistory` 做"见得少/答错过的优先、连对的降权"——这是降低内容生产成本的杠杆，比堆题量更聪明。
2. **内容结构化的深度**：s1 的 derive-box（推导链）、probe-box（启发式追问）、mnemonic、trap-box、clinical-case 等，是"教得懂"的好设计，不是简单罗列。
3. **跨科关联数据基础已就绪**：3275 处 `[fj-]`/`[zy-]` 标记构成天然知识图谱，方剂↔中药↔病证的关系是本工具独有卖点。

## 四、关键缺口与风险（按严重度）

**🔴 风险1 — 用户系统是"写了后端、没接 App"**
- 事实：`cloudfunctions/` 下 login/register/initAdmin 是**真实实现**（含 bcrypt、限流、审计），但 `auth-client.js`/`sync-client.js` **不存在**，`index.html` 无任何 CloudBase SDK 引用。
- 影响：多设备同步、多用户隔离、学习数据云端持久化 = 全部未生效。设计文档很漂亮，但产品里完全没有。
- PM 判断：这是**"已承诺但未交付"**的头号问题。但更关键的是——**是否真的需要账号体系？** 若只是个人/小圈子备考，本地"导出/导入备份"比搭云后端更划算（见方向 P2）。

**🔴 风险2 — PWA 离线失效，文档却说可用（文档/代码漂移）**
- 事实：`sw.js` 存在但 `index.html` 已移除注册（handover 记录为"解决缓存问题"而去掉）。`DEPLOY_GUIDE.md` 仍声称"离线可用、断网也能用"——**对用户是误导**。
- 影响：手机"添加到主屏幕"可安装，但断网即用不了，违背离线学习的核心场景。
- PM 判断：与其因缓存 bug 彻底放弃 SW，不如修缓存策略（如只对 subjects/*.js、根 html/js 做 cache-first，内容更新走版本号）。这是"修复已承诺能力"。

**🟡 风险3 — 内容深度严重不均**
- s1 已升级到"推导链+追问+口诀"层级；但 `s4 方剂/s6 外科/s7 妇科/s8 儿科` **无 classic-quote/mnemonic**，动态题在这些科降级（解析无口诀）；`s3/s9` 知识点名过长、无口诀。
- 影响：学习体验因科目而异，弱科恰恰是考试重点（方剂、中药）。

**🟡 风险4 — 题库薄且不均**
- 中药 23、方剂 16、针灸 18 题偏少。动态引擎能补，但低覆盖科"同一题反复出"风险高。

**🟡 风险5 — b*/c* 内容目录状态不明（~102 个 HTML 片段）**
- 这些是 AI 生成的增强内容片段（含 derive-box/mnemonic/trap-box，如 `c_5_2_5` 讲"脾为生痰之源"）。
- **需确认**：这些片段是否已是"待合并到科目文件的生产物料"？若是，则存在一个**未排期的内容集成 backlog**；若已合并，则可归档。这是当前最大的"信息黑盒"。

**🟢 风险6 — 无版本控制 / 无回归测试**
- 项目无 git；仅 `node --check` 语法校验。迭代越多越易回归。建议初始化 git + 一份轻量冒烟测试。

## 五、后续迭代方向（优先级排序）

> 原则：先**补完已承诺能力**与**内容均质化**（保住核心价值），再上**差异化新功能**；新功能严格按"用户价值×可行性"取舍。

### P0 — 修复"承诺但未落地"（1-2 周）
- **P0-1 离线 PWA 复活**：修复并重新注册 `sw.js`（cache-first 仅限静态资源 + 版本号失效），让"断网刷题"名副其实；同步修正 `DEPLOY_GUIDE.md` 漂移。
- **P0-2 厘清 b*/c* 内容 backlog**：与用户确认这批片段的去向——若需合并，排期分批并入 s5/s6/s7/s8 等弱科，并归档源文件。
- **P0-3 初始化 git** + 一份本地冒烟测试脚本（加载 9 科 + 出题 + 跨科跳转）。

### P1 — 内容均质化 + 题库增厚（高用户价值，2-4 周）
- **P1-1 弱科升级**：将 s4/s6/s7/s8/s9 提升到 s1 层级（补 mnemonic / classic-quote / probe-box），直接提升备考效果。
- **P1-2 薄题库增厚**：优先补 中药/方剂/针灸 到每科 ≥40 题，并带 `pointId`，让自适应练习更稳。
- **P1-3 跨科知识图谱落地（xlink C）**：渲染 `xgraph.js`，可视化"某方剂的组成中药 / 某病证对应的方剂群"。这是竞品少有、本工具数据天然支持的能力，**建议优先于用户系统**。

### P2 — 以"低成本替代"满足真实需求（战略取舍）
- **P2-1 本地备份/恢复（替代云账号）**：导出/导入 JSON（含 perf/错题/进度），解决"换设备丢进度"痛点，零后端成本。
- **P2-2 本地学习分析增强**：在现有 `perf` 上做"薄弱点热力图 + 备考就绪度预测 + 连续学习天数"，无需云端即可给用户反馈。
- **用户系统（云后端）降级为"按需"**：仅当你明确要**分发给药徒弟/小班多人使用**时才做完整账号体系；否则 P2-1 已解决绝大多数痛点。

### P3 — 长期差异化（验证后再投）
- **P3-1 正式间隔重复（SM-2）**：在现有"连对降权"基础上做科学复习排程，强化长期记忆。
- **P3-2 全真模考模式**：按考试大纲蓝图出套卷、计时、对照合格线评分。
- **P3-3 AI 辅导问答**：基于已结构化内容做答疑——**需警惕考试诚信与幻觉风险**，建议仅作"知识点讲解"而非"押题"。
- **P3-4 微信小程序分发**：考生群体在微信生态，但系重写成本高；PWA 够用前不建议。

## 六、需要你拍板的战略决策点

1. **分发模式**：本工具是**自用/小圈子**，还是要**分发给一批师承考生**？这直接决定 P2-1（本地备份）是否够用，还是必须上 P2 的云账号体系。
2. **b*/c* 内容片段**：这批 ~102 个片段是**待合并的生产物料**还是**已废弃的中间产物**？决定是否有内容集成 backlog。
3. **跨科图谱优先级**：是否同意"先落地知识图谱（P1-3）"而非"先接用户系统"？我倾向前者——它复用已有数据、零后端、且是独有卖点。

## 七、建议的下一步（若认同上述方向）

1. 今天即可做：初始化 git、修正 DEPLOY_GUIDE 漂移、确认 b*/c* 归属。
2. 本周：复活离线 PWA（P0-1）。
3. 排期：弱科内容升级 + 跨科图谱（P1），作为下个迭代主线。

---
*附：评估依据为项目内文档（PROJECT_HANDOVER、DESIGN_MASTER、跨科检索设计、用户系统设计、QUICK_START、DEPLOY_GUIDE）与代码事实（app.js/index.html/cloudfunctions/xlink.js 实际状态）交叉核实。*
